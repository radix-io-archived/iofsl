

This couple of modules will download the git repo for IOFSL.

ConfigObject needs to be installed on your system for it to 
function properly.  

What we see here is likely to change in future through refactors.

order of operations:

Install ConfigObj

python Make_Config.py

python Downld_Repo.py -r git1


